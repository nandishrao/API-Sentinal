// Phase 5 builds this out: table input -> POST /api/runs -> categorized
// results view (breaking / non-breaking / ambiguous) + summary + run history.
// Left as a placeholder in Phase 1 so the scaffold runs end-to-end on `npm run dev`.
export default function App() {
  return (
    <div style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>API Breaking Change Detector</h1>
      <p>Phase 5 builds the upload/results UI here.</p>
    </div>
  );
}
